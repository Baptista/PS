@model IEnumerable<Gamestats.Models.GameSetUpModels>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.stadium)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.formation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nameHomeTeam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nameAwayTeam)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.stadium)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.formation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nameHomeTeam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nameAwayTeam)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
