@model IEnumerable<GameObserver.Models.TeamModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Data)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdClub)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdFormation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Data)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdClub)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdFormation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { date=item.Data,idclub=item.IdClub/* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new {date=item.Data,idclub=item.IdClub,idformation=item.IdFormation }) |
            @Html.ActionLink("AdjustPosition","AdjustTeam","Team")|
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
